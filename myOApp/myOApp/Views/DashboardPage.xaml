<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="myOApp.Views.DashboardPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:myOApp.Controls"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="clr-namespace:myOApp.Extensions"
    Title="MyOApp"
    BackgroundColor="{DynamicResource MainBackgroundColor}"
    mc:Ignorable="d"
    >

    <!-- Sessions page for handling loading and empty message-->

    <ScrollView>
        <StackLayout BackgroundColor="{DynamicResource MainBackgroundColor}">
            <StackLayout Padding="10">
                <Label FontSize="Large" Text="Favorited events" />
            </StackLayout>
            <ScrollView MinimumHeightRequest="200" Orientation="Horizontal">
                <FlexLayout BindableLayout.ItemsSource="{Binding FavoritedEvents}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="10,10" FlexLayout.AlignSelf="Start">
                                <controls:EventCardView />
                            </Grid>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </FlexLayout>
            </ScrollView>
            <Grid Padding="10" MinimumHeightRequest="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label 
                    FontSize="Large" 
                    Text="Happening nearby"
                    Grid.Column="0" />
                <!--  Todo: Fix the icon size on iOS, on Android also it's not best => Yanli  -->
                <!--  Todo: Use https://forums.xamarin.com/discussion/72417/setting-tint-color-of-an-image FFImageLoading and change color of icon  -->
                <Label
                    FontFamily="FA-S"
                    FontSize="Large"
                    Text="{StaticResource Settings}"
                    Grid.Column="1"
                    HorizontalTextAlignment="Start"
                    VerticalTextAlignment="Center">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoToSettingsCommand}" />
                    </Label.GestureRecognizers>
                </Label>
            </Grid>
            <CollectionView ItemsSource="{Binding NearbyEvents}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout ItemSpacing="1" Orientation="Vertical" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="10" BackgroundColor="{DynamicResource CardBackgroundColor}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!--  Event date  -->
                            <Label
                                Grid.Row="0"
                                Grid.RowSpan="3"
                                Grid.Column="0"
                                FontSize="54"
                                FontFamily="Roboto"
                                HorizontalTextAlignment="Center"
                                Text="31"
                                TextColor="{DynamicResource AccentColor}"
                                VerticalTextAlignment="Center" />
                            <Label
                                Grid.Row="2"
                                Grid.RowSpan="2"
                                Grid.Column="0"
                                FontSize="22"
                                HorizontalTextAlignment="Center"
                                Text="SEP"
                                TextColor="{DynamicResource AccentColor}"
                                VerticalTextAlignment="Center" />

                            <!--  Event info  -->
                            <Label
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                Grid.Column="1"
                                FontSize="17"
                                HorizontalTextAlignment="Start"
                                LineBreakMode="WordWrap"
                                Style="{DynamicResource CardLabel}"
                                Text="45. Swiss International Mountain Marathon"
                                VerticalTextAlignment="End" />
                            <Label
                                Grid.Row="2"
                                Grid.Column="1"
                                FontSize="15"
                                HorizontalTextAlignment="Start"
                                LineBreakMode="TailTruncation"
                                Style="{DynamicResource CardLabel}"
                                Text="Map: Konstanz/Kreuzlingen"
                                VerticalTextAlignment="End" />
                            <Label
                                Grid.Row="3"
                                Grid.Column="1"
                                FontSize="13"
                                HorizontalTextAlignment="Start"
                                LineBreakMode="TailTruncation"
                                Style="{DynamicResource CardLabel}"
                                Text="GL/GR | OLK Argus, Argus Bikers"
                                VerticalTextAlignment="Start" />

                            <!--  Favicon  -->

                            <Label 
                Grid.RowSpan="4"
                Grid.Column="2"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                FontFamily="FA-S"
                FontSize="26"
                Text="{StaticResource Heart}"
                >
                                <Label.TextColor>
                                    <Binding Path="IsFavorite">
                                        <Binding.Converter>
                                            <converters:BoolToObjectConverter x:TypeArguments="d:Color"
                                                              TrueObject="{StaticResource FavoritedColor}"
                                                              FalseObject="{StaticResource NotFavoritedColor}" />
                                        </Binding.Converter>
                                    </Binding>
                                </Label.TextColor>
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding ToggleFavoriteItemCommand}" />
                                </Label.GestureRecognizers>
                            </Label>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                <CollectionView.EmptyView>
                    <StackLayout BackgroundColor="{DynamicResource CardBackgroundColor}">
                        <Label
                            Margin="10,25,10,10"
                            FontAttributes="Bold"
                            FontSize="18"
                            HorizontalOptions="Fill"
                            HorizontalTextAlignment="Center"
                            Text="No events are happening near you." />
                        <Label
                            FontSize="12"
                            HorizontalOptions="Fill"
                            HorizontalTextAlignment="Center">
                            <Label.FormattedText>
                                <FormattedString>
                                    <FormattedString.Spans>
                                        <Span Text="Try " />
                                        <Span
                                            FontAttributes="Italic"
                                            Text="choosing more regions?"
                                            TextColor="{DynamicResource AccentColor}">
                                            <Span.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding GoToSettingsCommand}" />
                                            </Span.GestureRecognizers>
                                        </Span>
                                    </FormattedString.Spans>
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                    </StackLayout>
                </CollectionView.EmptyView>
            </CollectionView>
        </StackLayout>
    </ScrollView>
</ContentPage>
