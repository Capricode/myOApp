<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="myOApp.Views.AboutPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    xmlns:local="clr-namespace:myOApp.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{Binding PersonalizedTitle}"
    BackgroundColor="{DynamicResource MainBackgroundColor}"
    mc:Ignorable="d">

    <!--  Todo: Rethink the background color  -->
    <Grid BackgroundColor="{DynamicResource MainBackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label
            Grid.Row="0"
            Grid.Column="0"
            Padding="10"
            FontSize="Large"
            Text="What's your name?" />
        <Entry
            Grid.Row="0"
            Grid.Column="1"
            Placeholder="Your name..."
            Text="{Binding Name}" />

        <Label
            Grid.Row="1"
            Grid.Column="0"
            Padding="10"
            FontSize="Large"
            Text="Choose layout:" />
        <local:EnumPicker
            Title="Select Theme"
            Grid.Row="1"
            Grid.Column="1"
            Margin="10"
            ios:Picker.UpdateMode="WhenFinished"
            EnumType="{x:Type local:ThemeEnum}"
            SelectedIndexChanged="OnThemeChanged"
            SelectedItem="{Binding Theme}"
            TextColor="{DynamicResource ContrastTextColor}" />

        <Label
            Grid.Row="2"
            Grid.Column="0"
            Padding="10"
            FontSize="Large"
            Text="Pick regions" />
        <ListView
            Grid.Row="3"
            Grid.ColumnSpan="2"
            Margin="10"
            ItemsSource="{Binding RegionsData}"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackLayout VerticalOptions="CenterAndExpand">
                                <Label Text="{Binding Data.Name}" />
                                <Label FontSize="10" Text="{Binding Data.Description}" />
                            </StackLayout>
                            <CheckBox Grid.Column="1" IsChecked="{Binding Selected}" />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</ContentPage>
