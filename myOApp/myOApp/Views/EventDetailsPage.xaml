<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:MyOApp.Controls"
             mc:Ignorable="d"
             x:Class="myOApp.Views.EventDetailsPage">
    <ContentPage.Content>
        <StackLayout Margin="20,20,20,25">
            <controls:EventLabel Label="Name" Text="{Binding Name}" />
            <controls:EventLabel Label="Date" Text="{Binding Date, StringFormat='{0:dd.MM.yy}'}" />
            <controls:EventLabel Label="Map" Text="{Binding Map}" />
            <controls:EventLabel Label="Club" Text="{Binding Club}" />
            <controls:EventLabel Label="Region" Text="{Binding Region}" />

            <Button Text="Show results (opens a browser)" IsVisible="{Binding HasResults}" Command="{Binding GoToBrowserCommand}" CommandParameter="{Binding ResultsUrl}" />
            <Button Text="Show event page (opens a browser)" Command="{Binding GoToBrowserCommand}" CommandParameter="{Binding Link}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>